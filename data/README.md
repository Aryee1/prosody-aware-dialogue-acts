# Data layout (not committed)

This repo does not include AMI data files.

Expected local folders:

- `data/raw/ami/`  
  AMI manual annotations extracted here (e.g., `dialogueActs/`, `words/`, `ontologies/`).

Generated by scripts:

- `data/processed/`  
  Derived utterance table + train/val/test splits.